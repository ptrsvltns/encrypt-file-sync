const inquirer = require("inquirer");const fs = require("fs-extra");module.exports = {  config: () => {    return inquirer.prompt([      {        type: "list",        name: "config",        message: "选择配置",        choices: fs.readdirSync('config').filter((filename) => filename.endsWith(".json")),        filter: function (val) {          return val;        }      }    ]);  },}